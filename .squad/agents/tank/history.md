# Tank — History

## Project Context
- **Project:** azure-hub-spoke — Hub-and-Spoke networking labs on Azure.
- **Key fact:** Each top-level folder is a distinct, self-contained lab scenario with its own deploy/validate/cleanup scripts.
- **Stack:** Azure CLI (`.azcli`, `.sh`), ExpressRoute, VPN Gateway, Azure Route Server, OPNSense NVA, GCP/Megaport.
- **Created:** 2026-06-04

## Learnings
- 2026-06-04: er-migration lab scripts currently expose hygiene risks: plaintext VM password in 1-hub-spk.sh, unpinned remote ARM template URL, hardcoded GCP project/regions, no shared vars file, missing shebang/strict mode, and scratch validation commands in 4-validation.sh.
- 2026-06-04: For er-migration, recommend a shared lab.env plus validation-first wrappers, Key Vault/secret parameters for VM credentials, pinned template/image versions, robust wait helpers, tags/labels, and explicit dry-run cleanup guidance.
- 2026-06-04: Archived 6 original imperative shell/gcloud scripts (1-hub-spk.sh, 2-gcp-er.sh, 3-add-subnetprefix.sh, 4-validation.sh, 5-clean-up.sh, gw-prepare.png) to archive/ with README; Terraform rebuild now primary source.
- 2026-06-04: Scaffolded er-migration Terraform root files from the authoritative address-plan contract; modules remain owned by other agents.
- 2026-06-04: Built er-migration Terraform modules for Azure hub, spokes, and Route Server using exact corrected CIDRs and Azure-required subnet names.
- 2026-06-06: deploy.ps1 and deploy.sh now interactively prompt for Azure region (default: westus3) and GCP region (default: us-east1, only when GCP is in scope). Both prompts are bypassable via -AzureRegion/-GcpRegion (PowerShell) and --azure-region/--gcp-region (Bash). The chosen Azure region wires to the Terraform top-level variable `location`; the GCP region wires to the top-level variable `gcp_region` (both written into terraform.tfvars). GCP region is NOT nested inside the `gcp_onprem` object variable. Quality gates passed: deploy.ps1 ParseFile errs.Count == 0; deploy.sh bash -n exit 0, 0 CR bytes.
- 2026-06-06: Region-prompt changes (d87e53f) committed and pushed to origin/main. ⚠️ CRITICAL: er-migration/terraform/terraform.tfvars and *.tfstate files are gitignored; NEVER commit them. These are local machine state and credentials files and must remain gitignored indefinitely.
- 2026-06-06: README Excalidraw-link change (57ead7a) committed and pushed to origin/main. Added "open in Excalidraw" deep link under architecture diagram in er-migration README using excalidraw.com/#url pattern for web-based editing.
- 2026-06-06: Added automatic ER connection self-heal to both deploy scripts. Function names: `Repair-FailedErConnection` (deploy.ps1) and `repair_failed_er_connection` (deploy.sh). Proven fix sequence encoded: (1) confirm circuit serviceProviderProvisioningState == Provisioned (guard — never recreate against an unprovisioned circuit), (2) az network vpn-connection delete --yes to remove the Failed connection, (3) terraform apply so TF owns the resource, (4) Wait-ForState/wait_for_state poll to Succeeded (bounded 20 min / 30 s interval), (5) az network vnet-gateway list-learned-routes to verify ERGW learned the GCP prefix 192.168.100.0/24. Phase 5 (pre-apply orphan check) strengthened: circuit guard added, interactive prompt removed (auto-delete when circuit is Provisioned). Function invoked: after Phase 7 poll if connection is Failed (up to 2 remediation attempts), and ERGW route check runs in Phase 8 regardless of GCP mode. Quality gates: deploy.ps1 ParseFile errs.Count == 0; deploy.sh bash -n exit 0, 0 CR bytes.
- 2026-06-06: Created cleanup.ps1 and cleanup.sh in er-migration/scripts/ as the primary teardown pair, mirroring deploy.ps1/deploy.sh in style, logging, params, and phase-banner format. Order of operations: (1) confirmation + prereq/auth check, (2) pre-destroy: delete orphan ER connection 'az-hub-ergw-to-az-hub-er-circuit' via az (connection may exist outside TF state from incident remediation — TF destroy will NOT remove it), (3) terraform destroy with same vars as deploy (location, gcp_project, gcp_region, gcp_onprem.deploy_gcp, TF_VAR_admin_password), (4) post-destroy verification: az group show / az resource list, warn on leftovers, (5) final Megaport VXC manual deletion reminder. Flags: -Force/-SkipGcp/-AzureRegion/-GcpProject/-GcpRegion (PS); --force/--yes/--skip-gcp/--azure-region/--gcp-project/--gcp-region (Bash). Quality gates: cleanup.ps1 ParseFile errs.Count == 0; cleanup.sh 0 CR bytes, bash -n exit 0. README updated: cleanup.ps1/cleanup.sh added to repo-layout tree and security/cost notes rewritten with usage examples and the orphan-connection caveat.
- 2026-06-06: ER connection self-heal + Azure+GCP cleanup scripts committed and pushed to main (commit 6c507ff). 5 files committed: er-migration/README.md, er-migration/scripts/deploy.ps1, er-migration/scripts/deploy.sh, er-migration/scripts/cleanup.ps1, er-migration/scripts/cleanup.sh. Changes staged explicitly (no broad globs); .squad/ files not included per Scribe policy.
